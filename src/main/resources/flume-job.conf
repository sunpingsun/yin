# Define agent Name
a1.sources = r1
a1.sinks = k1 k2
a1.channels = c1 c2

# 1. Source: Netcat
a1.sources.r1.type = netcat
a1.sources.r1.bind = localhost
a1.sources.r1.port = 10050

# Selector: Replicating (Multiplexing to both channels)
a1.sources.r1.selector.type = replicating
a1.sources.r1.channels = c1 c2

# 2. Channel 1 (for Kafka)
a1.channels.c1.type = memory
a1.channels.c1.capacity = 10000
a1.channels.c1.transactionCapacity = 100

# 3. Channel 2 (for HDFS)
a1.channels.c2.type = memory
a1.channels.c2.capacity = 10000
a1.channels.c2.transactionCapacity = 100

# 4. Sink 1: Kafka
a1.sinks.k1.type = org.apache.flume.sink.kafka.KafkaSink
a1.sinks.k1.kafka.topic = order
a1.sinks.k1.kafka.bootstrap.servers = 192.168.12.41:9092
a1.sinks.k1.kafka.flumeBatchSize = 20
a1.sinks.k1.kafka.producer.acks = 1
a1.sinks.k1.channel = c1

# 5. Sink 2: HDFS
a1.sinks.k2.type = hdfs
# Adjust port 8020 if necessary (e.g., to 9000) based on actual Hadoop config
a1.sinks.k2.hdfs.path = hdfs://192.168.12.41:8020/user/test/flumebackup
a1.sinks.k2.hdfs.filePrefix = events
a1.sinks.k2.hdfs.fileType = DataStream
a1.sinks.k2.hdfs.writeFormat = Text
# Roll every 60 seconds or 128MB
a1.sinks.k2.hdfs.rollInterval = 60
a1.sinks.k2.hdfs.rollSize = 134217728
a1.sinks.k2.hdfs.rollCount = 0
a1.sinks.k2.channel = c2
